inserter = require '../run'

describe 'Insert keys', ->

  describe 'Insert values of same size', ->
    it 'after inserted a lot of values in small size, chunks ran out, eviction happened.', (done) ->
      done()

    it 'after inserted a lot of values in bigger size, chunks ran out, eviction happened.', (done) ->
      done()

  describe 'Insert values of varying sizes', ->
    it 'after inserted a lot of values of varying sizes, chunks ran out, eviction happened.', (done) ->
      done()


