inserter = require '../run'

describe 'Insert keys', ->

  describe 'Insert values of same size', ->
    it 'after inserted a lot of values in small size, eviction rate increased, although the memcache usage is pretty low.', (done) ->
      done()

    it 'after inserted a lot of values in bigger size, eviction rate increased, although the memcache usage is pretty low', (done) ->
      done()

  describe 'Insert values of varying sizes', ->
    it 'after inserted a lot of values of varying sizes, eviction rate increased, but the usage is much higher', (done) ->
      done()


